server:
  port: 8761

spring:
  application:
    name: discoveryservice

eureka:
  client:
    register-with-eureka: false # 유레카 서버 자체는 테이블에 등록하지 않겠다는 말(다른 마이크로서비스 등에서 참조할 필요가 있을때 정도에 사용)
    fetch-registry: false